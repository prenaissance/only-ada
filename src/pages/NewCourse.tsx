import CreateTrainingForm from "@features/trainings/components/CreateTrainingForm";

type Props = {};

const NewCourse = (props: Props) => {
  return <CreateTrainingForm />;
};

export default NewCourse;
